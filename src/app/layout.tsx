import './globals.css'
import type { Metadata } from 'next'
import { Inter } from 'next/font/google'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'Ollama Document Anonymization - Solution de confidentialité pour avocats suisses',
  description: 'Application d\'anonymisation de documents juridiques avec confidentialité totale pour les professionnels du droit en Suisse.',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="fr">
      <body className={inter.className}>
        {children}
      </body>
    </html>
  )
}
